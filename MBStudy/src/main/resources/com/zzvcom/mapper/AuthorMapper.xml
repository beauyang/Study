<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzvcom.dao.AuthorMapper">
    <resultMap id="author" type="author">
        <id property="id" column="id" javaType="integer"></id>
        <result property="username" column="username" javaType="string"></result>
        <result property="password" column="password" javaType="string"></result>
        <result property="email" column="email" javaType="string"></result>
        <result property="bio" column="bio" javaType="string"></result>
        <result property="favouriteSection" column="favouriteSection" javaType="string"></result>
    </resultMap>
    <!--sql-->
    <insert id="addAuthor" parameterType="author">
        INSERT INTO t_author (id,username) VALUES (#{id},#{username})
    </insert>
    <insert id="insertAuthor" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO  t_author (username,password,email,bio,favouriteSection) VALUES
        (#{username},#{password},#{email},#{bio},#{favouriteSection})
    </insert>
    <delete id="deleteAuthor" parameterType="int">
        DELETE FROM t_author
        WHERE id = #{id}
    </delete>
    <select id="getAllAuthors" resultType="author">
        SELECT
        t.id,
        t.username,
        t.password,
        t.email,
        t.bio,
        t.favourite_section favouriteSection
        FROM author t
    </select>
    <select id="getAllAuthorsCount" resultType="int">
        SELECT count(1)
        FROM t_author
    </select>
    <update id="updateAuthor" parameterType="author">
        UPDATE t_author
        SET username = #{username}
    </update>
    <select id="findById" resultMap="author" parameterType="int">
        SELECT *
        FROM t_author
        WHERE id =#{id}
    </select>
</mapper>